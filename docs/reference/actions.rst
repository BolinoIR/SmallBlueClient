Action reference
================

All BBB mutations are embedded in :mod:`sbc.operations`. The release build also
generates ``sbc/API.md`` and typing signatures from the same action definitions.

.. automodule:: sbc.operations
   :members: Actions
